[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "veil-garden"
version = "1.0.1"
description = "A local-first organizer for user-provided Hide My Email aliases."
readme = "README_EN.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "ferretgeek" }]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Environment :: Web Environment",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Topic :: Communications :: Email",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/ferretgeek/VeilGarden"
Issues = "https://github.com/ferretgeek/VeilGarden/issues"

[project.scripts]
veil-garden = "veil_garden.__main__:main"

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
veil_garden = ["static/*"]

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
