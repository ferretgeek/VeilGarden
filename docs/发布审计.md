# 发布审计

审计日期：2026-08-09

目标版本：v1.0.1（v1.0.0 的不可变发布记录继续保留）

范围：私人原件、公开工作树、拟提交 Git 历史、依赖、容器、界面、图片、文档、GitHub 设置与未登录在线视角。

## 私人原件门禁

- [x] 递归读取全部 6 个 TXT，并识别服务器登录资料、Apple 主账号资料和真实地址清单。
- [x] 私人原件以顶层拒绝式 `.gitignore` 保护；脱敏审查保留在原目录，原数据未删除。
- [x] 原项目的 Apple 密码/2FA/会话、无鉴权公网面板、通配 CORS、明文令牌、密码式 SSH、硬编码部署身份、私有协议和自动任务均未复制。
- [x] 公开版由空目录独立重建，功能边界改为 Apple 官方流程的本地整理伴侣。

## 本地门禁

- [x] 全部 58 项 Python 与浏览器契约测试通过；Python / JavaScript 语法通过。
- [x] Ruff 格式与静态检查通过；Bandit 中高危检查、运行/开发依赖 pip-audit 通过。
- [x] detect-secrets 与 Gitleaks 当前文件、暂存内容、完整 Git 历史和发布源代码包均为零结果。
- [x] Wheel 构建并在全新虚拟环境安装复测；18 个包内文件均在允许范围内。
- [x] Dockerfile 已由 GitHub CI 真实构建；Compose、systemd、Nginx 与 GitHub Workflow 已通过静态复核和 YAML 解析。
- [x] 实际桌面与 390×844 移动端验证访问门禁、遮罩/揭示、搜索、导入、添加、编辑、主题、完整导出确认和本地移除确认；移动端无横向溢出。
- [x] 四套全局主题与持久化通过；深灰背景实测为 `#17191d`。
- [x] SVG、PNG、ICO 收藏图标齐全；1280×720 实际界面图与 1280×640 社交预览只含保留示例数据，且无附加图片元数据。
- [x] 46 文件干净克隆复测 58/58；所有 Markdown 相对链接和图片可解析。

## GitHub 门禁

- [x] 公开仓库、`main` 默认分支、双语仓库描述、7 个 Topics 与 MIT 许可证在线核验。
- [x] 主分支 CI、CodeQL、Docker 容器构建与 Dependency Graph 在线成功。
- [x] Dependabot、Secret Protection、Push protection、私密漏洞报告和依赖图在线核验。
- [x] 1280×640 Social preview 已上传并在线按原尺寸加载；README 实际界面图在线加载。
- [x] 不可变 v1.0.0 Release 已创建并带 GitHub attestation；标签目标为 `8a6f69767cc841cefcd2e68685b9d3f5ca638230`。GitHub 生成源码 ZIP 含 46 个文件，SHA-256 为 `c2a860d0be7d2c69f7ac01fc6fb9ee9d0c620d74e8a3820c8d995850eace0724`。
- [x] GitHub 个人主页第 14 张双语卡片、独立 1280×640 预览图与链接已上线；个人简介原文逐字核验未改。
- [x] 工作区根 `README.md` 已同步项目、仓库、图片、15/14 仓库计数与核验范围。

只有全部门禁完成并在线复核后，才可将本文件视为发布闭环记录。

## v1.0.1 稳定性复核

公开远端新克隆在 Windows 上暴露出写入请求被拒时的连接重置竞态，先后复现在跨端口 Origin 与错误 Content-Type 路径。根因是服务端在请求体未读完时提前返回错误；修复后会在 Host 与令牌通过后先一次性读取最多 256 KiB 的请求体，再执行 Origin、Content-Type 和业务校验，确保返回确定的错误响应，同时不为未认证请求扩大读取成本。

- [x] 跨端口拒绝契约连续运行 20 轮、错误 Content-Type 契约连续运行 20 轮全部通过；拒绝请求不会创建本地记录。
- [x] 修复后全部 58 项测试、Ruff、Bandit、detect-secrets、Gitleaks 当前文件和 v1.0.1 Wheel 全新目录安装通过。
- [x] 修复提交的 5 个提交完整历史双扫描为零；GitHub 公开新克隆连续两轮通过 116 项测试，Windows 本地与 Linux CI、CodeQL、Docker 构建均通过。
- [x] 不可变 v1.0.1 Release 已发布，标签目标为 `1e5d4e176a6ba95b3a66cbf7771e7ce527b7c46c`，GitHub attestation 已生成；46 文件源码 ZIP 双扫描为零，SHA-256 为 `86ebc2a1b9dcd409f2f4d603dc1f3c87ac3dd15043b2d31fd4663c1915432c4f`。
- [x] 工作区根总览已记录 v1.0.1；GitHub 主页最新版本徽章与第 14 张双语卡片在线加载，个人简介原文逐字核验未改。
